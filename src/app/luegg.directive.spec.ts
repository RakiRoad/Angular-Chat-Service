import { LueggDirective } from './luegg.directive';

describe('LueggDirective', () => {
  it('should create an instance', () => {
    const directive = new LueggDirective();
    expect(directive).toBeTruthy();
  });
});
